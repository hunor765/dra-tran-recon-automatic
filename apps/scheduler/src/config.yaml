ga4:
  property_id: "YOUR_PROPERTY_ID"
  credentials_file: "credentials.json"

backend:
  # Selected Platform: woocommerce OR shopify
  platform: "woocommerce"
  
  woocommerce:
    url: "https://your-store.com"
    consumer_key: "ck_..."
    consumer_secret: "cs_..."
    
  shopify:
    shop_url: "your-shop.myshopify.com"
    access_token: "shpat_..."

reconciliation:
  match_window_days: 2  # 48h lookback as per plan
  currency_variance: 0.05

alerts:
  email:
    enabled: true
    sender: "reports@datarevoltagency.com"
    recipients:
      - "client@example.com"
      - "manager@datarevoltagency.com"
    smtp_server: "smtp.gmail.com"
    smtp_port: 587
